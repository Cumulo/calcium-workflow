# 对齐实时生命周期指南 / Align the real-time lifecycle guide

## 中文

- 根据 follow-up review 删除实时生命周期指南中的旧 `on-page-touch`/`.reconnect` 手动加速说明。
- 记录 Calcium cleanup-backed 应用 watcher 与 ws-edn transport cleanup 的独立所有权。
- 修正文档英文措辞，不改变运行语义。

## English

- Remove the obsolete `on-page-touch`/`.reconnect` manual-acceleration guidance after follow-up review.
- Document the separate ownership of Calcium's cleanup-backed application watcher and ws-edn's transport cleanup.
- Improve the documentation wording without changing runtime behavior.
