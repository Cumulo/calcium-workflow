# Calcit 0.13.78-rc.1 consumer validation / Calcit 0.13.78-rc.1 消费端验证

- Pin the Calcit CLI and `@calcit/procs` to the immutable `0.13.78-rc.1`
  prerelease and consume the fully converged published provider graph headed by
  cumulo-reel 0.0.37.
- Align `validate-server-patch` and all message decoder helpers with the canonical
  `Result<Value, Error>` generic order restored by calcit-lang/calcit#878, and
  correct the corresponding notes.
- 将 Calcit CLI 与 `@calcit/procs` 固定到不可变的 `0.13.78-rc.1` 预发布版本，
  并消费以 cumulo-reel 0.0.37 为首的完整已发布 provider 图。
- 按 calcit-lang/calcit#878 恢复的标准 `Result<Value, Error>` 泛型顺序修正
  `validate-server-patch` 与消息解码辅助函数，并同步更正文档记录。
- The public crates.io CLI and npm package both resolve at exact
  `0.13.78-rc.1`. Strict Caps installs and verifies all 16 formal modules with
  cumulo-util 0.0.18, ws-edn 0.0.26, Respo 0.16.95 and their matching released
  consumers; both client and server check-only gates pass without overrides.
- Browser validation found and corrected two stale `option:unwrap` calls inside
  `update` callbacks in `app.updater.user/log-in`: unlike `update-in` leaf
  callbacks, `update` already supplies the stored value. Three server/protocol
  regressions now pin the two callback contracts and the existing-session login,
  duplicate signup and message-removal paths.
- crates.io CLI 与 npm package 均以精确 `0.13.78-rc.1` 解析。严格 Caps 安装并验证
  16 个正式模块，包括 cumulo-util 0.0.18、ws-edn 0.0.26、Respo 0.16.95 及其
  匹配的已发布消费者；client/server check-only 均在无 override 下通过。
- 浏览器验收发现并修正 `app.updater.user/log-in` 的两处旧式二次
  `option:unwrap`：`update` 与 `update-in` leaf callback 的入参契约不同。新增三项
  server/protocol 回归，覆盖已有 session 登录、重复注册和消息删除路径。
