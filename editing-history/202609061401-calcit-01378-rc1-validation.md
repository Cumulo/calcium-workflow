# Calcit 0.13.78-rc.1 consumer validation / Calcit 0.13.78-rc.1 消费端验证

- Pin the Calcit CLI and `@calcit/procs` to the immutable `0.13.78-rc.1`
  prerelease while retaining the existing Reel and module dependency graph.
- Align `validate-server-patch` and all message decoder helpers with the canonical
  `Result<Value, Error>` generic order restored by calcit-lang/calcit#878, and
  correct the corresponding notes.
- 将 Calcit CLI 与 `@calcit/procs` 固定到不可变的 `0.13.78-rc.1` 预发布版本，
  保持现有 Reel 与模块依赖图不变。
- 按 calcit-lang/calcit#878 恢复的标准 `Result<Value, Error>` 泛型顺序修正
  `validate-server-patch` 与消息解码辅助函数，并同步更正文档记录。
- The public crates.io CLI and npm package both resolve at exact
  `0.13.78-rc.1`; strict Caps installs 16 formal modules and toolchain
  verification passes. Server check-only passes after the Result corrections.
- Client check-only remains blocked by four diagnostics from the formal
  transitive graph: cumulo-reel 0.0.36 still pins cumulo-util 0.0.17 and ws-edn
  0.0.25 instead of their published fixes 0.0.18 and 0.0.26. Keep this branch
  local until the separate Reel-owner release converges; do not shadow it with
  source overrides or a knowingly failing consumer PR.
- crates.io CLI 与 npm package 均以精确 `0.13.78-rc.1` 解析；严格 Caps 安装
  16 个正式模块且 toolchain 校验通过，Result 修正后 server check-only 通过。
- client check-only 仍被正式传递图的 4 条诊断阻断：cumulo-reel 0.0.36 仍固定
  cumulo-util 0.0.17 与 ws-edn 0.0.25，而已发布修复为 0.0.18/0.0.26。
  在独立 Reel owner 发版收口前保留本地分支，不使用源码覆盖或创建必红 PR。
