# Upgrade Calcium Workflow to Calcit 0.13.64

## 中文

- 将 `deps.cirru` 与 `@calcit/procs` 同步到 Calcit 0.13.64。
- 保持所有生态模块在当前已发布版本。
- 将服务端 session connect 操作改为直接调用具名 Enum：
  `schema/Op :session/connect`，作为 #524 修复的真实跨命名空间回归。

## English

- Align `deps.cirru` and `@calcit/procs` on Calcit 0.13.64.
- Keep all ecosystem modules on their current published versions.
- Change the server session-connect operation to the direct named Enum call
  `schema/Op :session/connect` as a real cross-namespace regression for #524.
