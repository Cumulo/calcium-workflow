{
  "dependencies": {
    "@calcit/procs": "^0.7.5"
  },
  "scripts": {
    "compile-page": "cr --emit-js --once",
    "release-page": "vite build --base=./",
    "watch-page": "cr --emit-js"
  },
  "devDependencies": {
    "bottom-tip": "^0.1.5",
    "url-parse": "^1.5.10",
    "vite": "^4.4.6"
  },
  "version": "0.0.1"
}
